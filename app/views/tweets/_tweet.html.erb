<div class='box'>
    <article class="media">
        <div class='media-left'>
            <figure class='image is-64x64'>
                <%= gravatar_image_tag(tweet.user.email, size: 64, alt: tweet.user.name) %>
            </figure>
        </div>

        <div class='media-content'>
                <div class='content'>
                    <strong><%= tweet.user.name %></strong><br />
                    <small>@<%= tweet.user.username %></small><br />
                <p>
                    <%= tweet.tweet %>
                </p>
            </div>
        </div>

        <nav class='level'>
            <div class='level-left is-mobile'>
                <%= link_to tweet, class: "level-item" do %>
                    <span class="icon"><i class="fa fa-link" aria-hidden='true'></i></span>
                <%end%>
                <% if  tweet.user.id == current_user&.id %>
                    <%= link_to edit_tweet_path(tweet), class: "level-item" do %>
                        <span class="icon"><i class="fa fa-pencil" aria-hidden='true'></i></span>
                    <%end%>
                    <%= link_to tweet, method: :delete, data: {confirm: "Are you sure you want to delete this tweet?"} , class: "level-item" do %>
                        <span class="icon"><i class="fa fa-trash-o" aria-hidden='true'></i></span>
                    <%end%>
                <%end%>
                <% if  user_signed_in? %>
                    <%= link_to like_path(tweet), class: "level-item" do %>
                        <span class="icon"><i class="fa fa-heart" aria-hidden='true'></i> </span> <%= tweet.likes.to_i %>
                    <%end%>
                <%else%>
                    <span class="icon"><i class="fa fa-heart" aria-hidden='true'></i> </span> <%= tweet.likes.to_i %>
                <%end%>
            </div>
        </nav>
    </article>
</div>